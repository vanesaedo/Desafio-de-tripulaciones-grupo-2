import React from "react";

const Nav = () => {

  /**
   * Componente Nav
   * 
   * @component
   * @returns {JSX.Element} El Nav renderizado
   */

  return <nav className="nav">
    <ul>
      <li>Logout</li>
    </ul>
  </nav>;
};

export default Nav;
